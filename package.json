{
  "name": "ai-solid-portfolio",
  "private": true,
  "engines": {
    "node": ">=26"
  },
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "pnpm --parallel --filter=\"./apps/*\" run dev",
    "build": "pnpm --recursive run build",
    "test": "pnpm --recursive run test",
    "lint": "pnpm --recursive run lint"
  }
}
